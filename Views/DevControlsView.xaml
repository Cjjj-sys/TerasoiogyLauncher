<UserControl x:Class="TerasoiogyLauncher.Views.DevControlsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TerasoiogyLauncher.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Styles/Button.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Border Background="#f6f7d4" Margin="0">
        <Grid Margin="0">
            <Button Content="NormalButton" FontSize="24" Height="70" Width="200" Style="{DynamicResource ButtonStyleNormal}"
                    HorizontalAlignment="Left" Margin="15" />
            <Button Content="X"
                    FontSize="24"
                    Height="70"
                    Width="200"
                    Style="{DynamicResource ButtonStyleClose}"
                    HorizontalAlignment="Right" Margin="15" />
        </Grid>
    </Border>
</UserControl>
