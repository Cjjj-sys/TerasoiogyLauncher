<UserControl x:Class="TerasoiogyLauncher.Views.DevTestView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:TerasoiogyLauncher.Views"
             xmlns:vm="clr-namespace:TerasoiogyLauncher.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Styles/Button.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Border Background="#f6f7d4">
        <Grid>
            <StackPanel Margin="5">
                <TextBox Text="{Binding ToExcuteCommand}" />
                <Button Content="Excute"
                        Command="{Binding ExcuteCommand}" />
                <TextBox Text="{Binding OutputResult}" />
                <Button Content="ShowOutputs"
                        Command="{Binding ShowOutputsCommand}" />
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
